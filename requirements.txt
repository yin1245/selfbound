# Required: recompute L0-L2 baselines from released session JSONLs (no API needed)
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
sentence-transformers>=2.2

# Optional: regenerate session traces from scratch (commercial-API models)
# openai>=1.0
# anthropic>=0.20
# dashscope>=1.14

# Optional: open-weights anchors (Mistral-7B, Llama-3-8B) via vLLM
# vllm>=0.4
# torch>=2.1
# transformers>=4.40

# Optional: held-out contamination control (MMLU-Pro / BBH on n=6 frontier)
# requires both ANTHROPIC_API_KEY and OPENAI_API_KEY env vars
# anthropic>=0.20
# openai>=1.0
